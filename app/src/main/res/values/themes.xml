<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.ClickNote" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:colorPrimary">@color/primary</item>
        <item name="android:colorPrimaryDark">@color/primary_variant</item>
        <item name="android:colorAccent">@color/secondary</item>
        
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        
        <!-- Navigation bar color. -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightNavigationBar">true</item>
        
        <!-- Enable RTL support -->
        <item name="android:supportsRtl">true</item>
        <item name="android:textAlignment">viewStart</item>
        <item name="android:layoutDirection">inherit</item>
    </style>

    <style name="Theme.ClickNote.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_clicknote_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.ClickNote</item>
        <item name="android:windowFullscreen">true</item>
    </style>
</resources> 